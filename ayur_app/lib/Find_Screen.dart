import 'package:flutter/material.dart';

class FindPlant extends StatelessWidget {
  const FindPlant({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Find Plant")),
    );
  }
}
